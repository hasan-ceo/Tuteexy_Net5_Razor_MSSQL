@model IEnumerable<Tuteexy.Models.ViewModels.RptCWAttendanceVM>
@{ ViewData["Title"] = "Classwork wise Attendance"; }


<partial name="_StatusMessage" for="@TempData["StatusMessage"]" />

<div class="container-fluid p-0">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="row justify-content-between">
                        <div class="col-6">
                            <h5 class="card-title">Classwork wise Attendance</h5>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="tblData" class="table table-striped table-sm table-hover table-full-width table-font-size">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Class</th>
                                    <th>Subject</th>
                                    <th>Title</th>
                                    <th>Student Name</th>
                                    <th>Date</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null)
                                {
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.ClassRoomName</td>
                                            <td>@item.Subject</td>
                                            <td>@item.Title</td>
                                            <td>@item.StudentName</td>
                                            <td>@item.WorkDate.ToString("dd/MMM/yyyy")</td>
                                            <td>@item.AttenStatus</td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script>$('#tblData').DataTable({
            "pageLength": 100,
            "ordering": false
        });</script>
}